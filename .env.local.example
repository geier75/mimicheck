# ------------------------------------------------------------------
# 🔒 LOKALE ENTWICKLUNG (.env.local)
# Diese Datei wird NICHT auf GitHub hochgeladen.
# Hier kommen deine echten Keys rein.
# ------------------------------------------------------------------

# 1. Supabase (Frontend Verbindung)
# Findest du in Supabase Dashboard -> Project Settings -> API
VITE_SUPABASE_URL=https://dein-projekt.supabase.co
VITE_SUPABASE_ANON_KEY=dein-anon-key-hier

# ------------------------------------------------------------------
# ⚠️ BACKEND KEYS (Edge Functions)
# Diese Keys gehören NICHT hier rein, sondern müssen via CLI gesetzt werden!
#
# Führe im Terminal folgenden Befehl aus, um sie zu setzen:
# npx supabase secrets set OPENAI_API_KEY=sk-...
# npx supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
# ------------------------------------------------------------------

# (Optional) Wenn du das Python Backend lokal startest:
OPENAI_API_KEY=sk-dein-openai-key
ANTHROPIC_API_KEY=sk-dein-anthropic-key
